import { image } from '@tensorflow/tfjs';
import React from 'react';
import {state: image} from './tabs/Home';

class Img extends React.Component {
    saveImg() {
        console.log(image)
    }
    render(){
        return(
            
        )
    }
}

export default Img